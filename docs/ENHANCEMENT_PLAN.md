# 系统全面增强方案 v2.0

> 基于2025年AI Agent最佳实践的系统升级
> 创建时间: 2025-12-24

## 概述

针对用户提出的5大核心需求，结合业界最佳实践，进行系统性升级。

---

## 一、Git-Based 双重记忆系统 (DiffMem架构)

### 问题分析
- ❌ 当前记忆系统与Git分离
- ❌ 每次恢复需重新分析项目
- ❌ 无法利用Git history作为上下文

### 解决方案: Git Context Controller (GCC)

```
┌─────────────────────────────────────────────────────────────┐
│                   Git-Based Memory System                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  工作区 (Working Directory)                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ .claude/TASKS.md        - 当前任务状态                │  │
│  │ .claude/SESSION.md      - 当前会话上下文               │  │
│  │ .claude/CONTEXT.md      - 活跃上下文快照               │  │
│  └──────────────────────────────────────────────────────┘  │
│                          ↓ git add & commit                 │
│  Git Commits (记忆节点)                                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ Commit 1: [研究] 完成需求分析                         │  │
│  │ Commit 2: [规划] PRD完成                              │  │
│  │ Commit 3: [架构] 系统设计完成                         │  │
│  │ Commit 4: [开发] F1功能完成                           │  │
│  │ ...每个里程碑都是记忆节点                              │  │
│  └──────────────────────────────────────────────────────┘  │
│                          ↓ git log + diff                   │
│  上下文恢复 (Context Recovery)                               │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 读取最近N个commits → 自动恢复上下文                   │  │
│  │ git diff → 感知所有变更                               │  │
│  │ 无需重新分析整个项目！                                 │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  Git Branches (并行探索)                                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ main           - 主线开发                             │  │
│  │ feature/auth   - 认证功能探索                         │  │
│  │ experiment/db  - 数据库方案测试                        │  │
│  │ → 不同方案可以并行尝试，失败了就删除分支               │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 核心机制

#### 1. Memory Commit (记忆提交)
```bash
# 自动触发时机
- 每完成一个任务
- 每通过一个Gate
- 每完成一个阶段
- 用户手动 /memory:commit

# Commit Message格式
[阶段][Agent] 任务描述

# 示例
git commit -m "[开发][开发者] 完成用户注册功能 (F1)

- 实现注册API
- 密码哈希
- 邮箱验证
- 单元测试

🧠 Memory Snapshot:
- 当前进度: 60%
- 下一任务: F2 用户登录
- 发现问题: 缺少输入验证 (#1)
"
```

#### 2. Context Recovery (上下文恢复)
```bash
/start --resume

→ 读取最近10个commits
→ 提取Memory Snapshot
→ 自动恢复:
  - 当前阶段
  - 活跃任务
  - 已完成任务
  - 发现的问题
  - 技术决策

→ 无需重新分析项目！
```

#### 3. Git Branch探索
```bash
# 尝试新方案
/branch create experiment/new-auth "尝试OAuth认证"

→ 创建新分支
→ 在新分支上开发
→ 如果成功: git merge
→ 如果失败: git branch -D
```

### 新增文件

1. **`.claude/CONTEXT.md`** - 活跃上下文快照
```markdown
# 活跃上下文快照

## 当前状态
- 阶段: 开发 (4/6)
- 进度: 60%
- Agent: 💻 开发者

## 活跃任务
| ID | 任务 | 状态 |
|----|------|------|
| F3 | 核心功能1 | 进行中 |

## 最近完成
- F1: 用户注册 ✅
- F2: 用户登录 ✅

## 发现的问题
- #1: 缺少输入验证

## 下一步
- 完成F3
- 修复#1
- 运行测试
```

2. **`.claude/GIT_MEMORY.json`** - Git记忆配置
```json
{
  "memoryCommits": {
    "autoCommit": true,
    "triggers": ["task_complete", "gate_pass", "phase_complete"],
    "includeSnapshot": true
  },
  "contextRecovery": {
    "commitDepth": 10,
    "autoRecover": true,
    "includeUncommitted": true
  },
  "branchStrategy": {
    "experimentBranches": true,
    "autoCleanup": true
  }
}
```

---

## 二、维护模式工作流 (Maintenance Mode)

### 问题分析
- ❌ 6阶段流程主要针对新项目
- ❌ 大部分时间都是维护/修复
- ❌ 缺少问题追踪系统

### 解决方案: 完整的维护模式

```
维护模式流程:
┌─────────────────────────────────────────────────────────┐
│  /start --type maintenance                              │
│  或                                                      │
│  /maintenance:init                                      │
└───────────────────────┬─────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  Step 1: 项目扫描                                        │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  - 扫描目录结构                                          │
│  - 读取现有CLAUDE.md                                     │
│  - 分析Git history (最近20个commits)                    │
│  - 检测未提交的变更                                       │
│  - 识别现有问题                                          │
└───────────────────────┬─────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  Step 2: 创建维护文件                                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  .claude/ISSUES.md     - 问题追踪                        │
│  .claude/SESSION_LOG.md - 维护会话日志                   │
│  .claude/CONTEXT.md    - 项目上下文快照                  │
└───────────────────────┬─────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  Step 3: 问题识别                                        │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  自动检测:                                               │
│  - TODO/FIXME注释                                       │
│  - 测试失败                                              │
│  - 类型错误                                              │
│  - 安全漏洞                                              │
│  - 性能问题                                              │
│                                                          │
│  用户报告:                                               │
│  - "帮我修复登录Bug"                                     │
│  - "优化数据库查询性能"                                   │
└───────────────────────┬─────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  Step 4: 问题优先级排序                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  | 优先级 | 问题 | 影响 | 工作量 |                      │
│  |--------|------|------|--------|                      │
│  | P0 | 登录崩溃 | 阻塞 | M |                           │
│  | P1 | N+1查询 | 性能 | L |                            │
│  | P2 | 缺少测试 | 质量 | XL |                          │
└───────────────────────┬─────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  Step 5: 逐个修复 + 追踪                                 │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  对每个问题:                                             │
│  1. 分析根因                                             │
│  2. 设计方案                                             │
│  3. 实现修复                                             │
│  4. 编写测试                                             │
│  5. Git commit (作为记忆节点)                            │
│  6. 更新ISSUES.md                                        │
└─────────────────────────────────────────────────────────┘
```

### 文件结构

#### `.claude/ISSUES.md` - 问题追踪
```markdown
# 问题追踪

> 项目: [项目名]
> 维护开始: 2025-12-24

## 🔴 高优先级 (P0 - 阻塞性)

| ID | 问题 | 发现时间 | 状态 | 负责Agent |
|----|------|----------|------|-----------|
| #1 | 登录接口500错误 | 12-24 14:00 | 🔄 修复中 | 💻 开发者 |

### #1 登录接口500错误

**症状**: 用户无法登录，返回500错误

**根因分析**:
- 数据库连接池耗尽
- 未正确关闭连接

**修复方案**:
1. 增加连接池大小
2. 使用with语句确保连接关闭
3. 添加连接超时

**修复进度**:
- [x] 根因分析
- [x] 方案设计
- [🔄] 代码实现
- [ ] 测试验证
- [ ] 部署上线

**相关Commits**:
- `abc123` - 增加连接池配置

---

## 🟡 中优先级 (P1 - 性能/体验)

| ID | 问题 | 发现时间 | 状态 | 负责Agent |
|----|------|----------|------|-----------|
| #2 | N+1查询问题 | 12-24 15:00 | ⬜ 待处理 | 💻 开发者 |

---

## 🟢 低优先级 (P2 - 优化/重构)

| ID | 问题 | 发现时间 | 状态 | 负责Agent |
|----|------|----------|------|-----------|
| #3 | 测试覆盖率不足 | 12-24 16:00 | ⬜ 待处理 | 🧪 测试员 |

---

## ✅ 已解决

(空)

---

## 统计

- 总问题数: 3
- 已解决: 0
- 进行中: 1
- 待处理: 2
```

#### `.claude/SESSION_LOG.md` - 维护会话日志
```markdown
# 维护会话日志

> 项目: [项目名]

## 2025-12-24 会话

### 15:00 - 开始维护模式
- 扫描项目发现3个问题
- 优先级排序完成
- 开始修复#1

### 15:30 - 修复#1进展
- 根因定位: 连接池耗尽
- 方案: 增加连接池 + with语句
- 代码已实现: `src/db.py:45`

### 16:00 - #1修复完成
- 测试通过 ✅
- Git commit: `abc123`
- 下一步: 修复#2

---

## 2025-12-23 会话

...
```

### 新增命令

#### `/maintenance:init` - 初始化维护模式
```bash
/project-optimizer:maintenance --init

→ 创建ISSUES.md
→ 创建SESSION_LOG.md
→ 扫描项目问题
→ 进入问题追踪模式
```

#### `/maintenance:scan` - 扫描问题
```bash
/project-optimizer:maintenance --scan

→ 检测TODO/FIXME
→ 运行测试
→ 检查类型错误
→ 安全扫描
→ 更新ISSUES.md
```

#### `/maintenance:fix` - 修复问题
```bash
/project-optimizer:maintenance --fix #1

→ 分析问题#1
→ 设计方案
→ 实现修复
→ 运行测试
→ Git commit
→ 更新ISSUES.md状态
```

---

## 三、多层级CLAUDE.md系统

### 问题分析
- ❌ 大项目单一CLAUDE.md太臃肿
- ❌ 上下文经常丢失
- ❌ 模块级信息难以管理

### 解决方案: 分层文档系统

```
项目根目录/
├── CLAUDE.md                      # 🌍 全局规则 (1级)
│   - 项目概述
│   - 技术栈
│   - 全局约定
│   - 架构原则
│
├── src/
│   ├── CLAUDE.md                  # 📦 模块规则 (2级)
│   │   - src目录职责
│   │   - 代码组织规范
│   │   - 导入规则
│   │
│   ├── auth/
│   │   ├── CLAUDE.md              # 🔒 子模块规则 (3级)
│   │   │   - 认证流程
│   │   │   - JWT规范
│   │   │   - 安全要求
│   │   │
│   │   ├── register.ts
│   │   └── login.ts
│   │
│   ├── api/
│   │   ├── CLAUDE.md              # 🌐 子模块规则 (3级)
│   │   │   - API设计规范
│   │   │   - 路由规则
│   │   │   - 错误处理
│   │   │
│   │   ├── users.ts
│   │   └── products.ts
│   │
│   └── database/
│       ├── CLAUDE.md              # 🗄️ 子模块规则 (3级)
│       │   - 数据库连接
│       │   - ORM使用
│       │   - 迁移流程
│       │
│       ├── models.ts
│       └── migrations/
│
├── tests/
│   └── CLAUDE.md                  # 🧪 测试规则 (2级)
│       - 测试策略
│       - 覆盖率要求
│       - Mock规范
│
└── .claude/
    ├── CONTEXT.md                 # 📸 活跃上下文
    ├── TASKS.md                   # 📋 任务追踪
    └── HIERARCHY.json             # 🗺️ 文档层级映射
```

### 自动上下文加载

```
Claude读取上下文顺序:
┌─────────────────────────────────────────┐
│ 1. .claude/CONTEXT.md (活跃上下文)       │
│    → 快速了解当前状态                    │
└───────────────────┬─────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│ 2. CLAUDE.md (根目录全局规则)           │
│    → 了解项目整体                        │
└───────────────────┬─────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│ 3. 当前模块的CLAUDE.md                  │
│    例如: src/auth/CLAUDE.md             │
│    → 了解模块具体规则                    │
└───────────────────┬─────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│ 4. Git最近10个commits                   │
│    → 了解最近的变更                      │
└─────────────────────────────────────────┘

总token: 约5K-10K (远小于重新分析整个项目)
```

### `.claude/HIERARCHY.json` - 文档层级映射

```json
{
  "hierarchy": [
    {
      "level": 1,
      "path": "CLAUDE.md",
      "scope": "global",
      "priority": "high",
      "description": "全局项目规则和架构"
    },
    {
      "level": 2,
      "path": "src/CLAUDE.md",
      "scope": "module",
      "priority": "high",
      "description": "源代码组织规范"
    },
    {
      "level": 3,
      "path": "src/auth/CLAUDE.md",
      "scope": "submodule",
      "priority": "medium",
      "description": "认证模块规则",
      "parent": "src/CLAUDE.md"
    },
    {
      "level": 3,
      "path": "src/api/CLAUDE.md",
      "scope": "submodule",
      "priority": "medium",
      "description": "API模块规则",
      "parent": "src/CLAUDE.md"
    },
    {
      "level": 2,
      "path": "tests/CLAUDE.md",
      "scope": "module",
      "priority": "medium",
      "description": "测试规范"
    }
  ],
  "autoLoad": {
    "enabled": true,
    "maxDepth": 3,
    "strategy": "context-aware"
  },
  "contextAssembly": {
    "order": ["active", "global", "current-module", "git-history"],
    "tokenBudget": 10000
  }
}
```

### 新增命令

#### `/hierarchy:init` - 初始化分层文档
```bash
/project-optimizer:hierarchy --init

→ 扫描项目结构
→ 建议CLAUDE.md位置
→ 生成HIERARCHY.json
→ 创建模板文件
```

#### `/hierarchy:load` - 智能加载上下文
```bash
/project-optimizer:hierarchy --load src/auth

→ 加载CLAUDE.md (全局)
→ 加载src/CLAUDE.md
→ 加载src/auth/CLAUDE.md
→ 加载最近10个commits
→ 组装完整上下文
```

---

## 四、真正的多Agent并行系统

### 问题分析
- ❌ 当前agents.md只是文档
- ❌ 没有真正的并行执行
- ❌ 没有Agent间通信

### 解决方案: LangGraph-inspired并行编排

```
多Agent并行架构:
┌─────────────────────────────────────────────────────────┐
│              Orchestrator (协调者)                       │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  职责:                                                   │
│  - 任务分配                                              │
│  - Agent调度                                             │
│  - 结果合并                                              │
│  - 冲突解决                                              │
└───────────────────────┬─────────────────────────────────┘
                        ↓
        ┌───────────────┴───────────────┐
        │                               │
┌───────▼────────┐              ┌───────▼────────┐
│   Agent Pool   │              │  State Graph   │
│ ━━━━━━━━━━━━━ │              │ ━━━━━━━━━━━━━ │
│                │              │                │
│ 💻 开发者 x3   │◀────────────▶│  共享状态:     │
│ 🧪 测试员 x2   │              │  - 代码变更    │
│ 👁️ 审核员 x1   │              │  - 测试结果    │
│ 📋 PM x1       │              │  - 问题列表    │
└────────────────┘              └────────────────┘
        │
        ↓ 并行执行
┌─────────────────────────────────────────────────────────┐
│  并行任务示例                                            │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                          │
│  任务组A (可并行):                                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │   T1     │  │   T2     │  │   T3     │             │
│  │  开发者1  │  │  开发者2  │  │  开发者3  │             │
│  │  Haiku   │  │  Sonnet  │  │  Sonnet  │             │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘             │
│       │             │             │                     │
│       └─────────────┴─────────────┘                     │
│                     ↓                                    │
│  任务组B (依赖A):                                        │
│  ┌──────────────────────────────┐                       │
│  │          T4                  │                       │
│  │        审核员                 │                       │
│  │        Sonnet                │                       │
│  └──────────────────────────────┘                       │
│                     ↓                                    │
│  任务组C (测试):                                         │
│  ┌──────────┐  ┌──────────┐                            │
│  │   T5     │  │   T6     │                            │
│  │  测试员1  │  │  测试员2  │                            │
│  │  Sonnet  │  │  Sonnet  │                            │
│  └──────────┘  └──────────┘                            │
└─────────────────────────────────────────────────────────┘
```

### 核心机制

#### 1. Agent Pool (Agent池)
```json
{
  "agentPool": {
    "developer": {
      "count": 3,
      "model": "dynamic",
      "concurrency": 3,
      "status": ["idle", "busy", "idle"]
    },
    "tester": {
      "count": 2,
      "model": "sonnet",
      "concurrency": 2,
      "status": ["idle", "idle"]
    },
    "reviewer": {
      "count": 1,
      "model": "sonnet",
      "concurrency": 1,
      "status": ["idle"]
    }
  }
}
```

#### 2. Parallel Execution (并行执行)
```python
# 伪代码示例
parallel_tasks = [
    {"id": "T1", "agent": "developer", "model": "haiku"},
    {"id": "T2", "agent": "developer", "model": "sonnet"},
    {"id": "T3", "agent": "developer", "model": "sonnet"}
]

# 并行启动3个Agent
results = await asyncio.gather(*[
    execute_task(task) for task in parallel_tasks
])

# 合并结果
merged_result = orchestrator.merge(results)
```

#### 3. State Sharing (状态共享)
```
StateGraph:
┌─────────────────────────────────────┐
│  Shared State                       │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                     │
│  codeChanges: [                     │
│    {file: "auth.ts", agent: "dev1"},│
│    {file: "api.ts", agent: "dev2"} │
│  ]                                  │
│                                     │
│  testResults: [                     │
│    {test: "auth", status: "pass"}  │
│  ]                                  │
│                                     │
│  conflicts: [                       │
│    {file: "shared.ts", agents: [...]}│
│  ]                                  │
└─────────────────────────────────────┘
```

### 实现方案

#### `.claude/AGENTS.json` - Agent配置
```json
{
  "orchestration": {
    "mode": "parallel",
    "maxConcurrency": 5,
    "conflictResolution": "auto"
  },
  "agents": {
    "developer": {
      "instances": 3,
      "capabilities": ["code", "refactor", "debug"],
      "modelStrategy": "dynamic"
    },
    "tester": {
      "instances": 2,
      "capabilities": ["unit-test", "integration-test"],
      "modelStrategy": "sonnet"
    },
    "reviewer": {
      "instances": 1,
      "capabilities": ["code-review", "security-audit"],
      "modelStrategy": "sonnet"
    }
  },
  "stateGraph": {
    "sharedState": [
      "codeChanges",
      "testResults",
      "issues",
      "conflicts"
    ],
    "conflictDetection": true,
    "autoMerge": false
  }
}
```

### 新增命令

#### `/agents:parallel` - 并行执行
```bash
/project-optimizer:agents --parallel T1,T2,T3

→ 分配3个开发者Agent
→ 并行执行任务
→ 实时监控进度
→ 自动合并结果
```

#### `/agents:status` - 查看Agent状态
```bash
/project-optimizer:agents --status

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Agent Pool Status
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💻 Developer (3):
  #1: 🟢 Idle
  #2: 🔴 Busy (T2)
  #3: 🔴 Busy (T3)

🧪 Tester (2):
  #1: 🟢 Idle
  #2: 🟢 Idle

👁️ Reviewer (1):
  #1: 🟢 Idle
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 五、综合最佳实践

### 上下文恢复流程

```
用户离开一周后回来:
┌──────────────────────────────────────┐
│ /start --resume                      │
└────────────────┬─────────────────────┘
                 ↓
┌──────────────────────────────────────┐
│ 1. Git-Based Context Recovery        │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ → 读取最近10个commits                 │
│ → 提取Memory Snapshots                │
│ → 检测未提交变更                      │
└────────────────┬─────────────────────┘
                 ↓
┌──────────────────────────────────────┐
│ 2. Hierarchical Doc Loading          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ → 加载CLAUDE.md (全局)                │
│ → 加载当前模块CLAUDE.md               │
│ → 加载.claude/CONTEXT.md              │
└────────────────┬─────────────────────┘
                 ↓
┌──────────────────────────────────────┐
│ 3. State Reconstruction               │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ → 当前阶段: 开发 (4/6)                │
│ → 进度: 60%                           │
│ → 活跃任务: F3                        │
│ → 已完成: F1, F2                      │
│ → 问题: #1 待修复                     │
└────────────────┬─────────────────────┘
                 ↓
┌──────────────────────────────────────┐
│ 4. Auto Continue                      │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ "欢迎回来！上次你完成了F2，             │
│  现在正在开发F3。是否继续？"            │
│                                       │
│ [1] ▶️ 继续F3                         │
│ [2] 📋 查看详情                        │
│ [3] 🔄 切换任务                        │
└──────────────────────────────────────┘

总耗时: < 5秒
总Token: < 10K
```

### 成本优化

```
对比:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

传统方式:
- 重新分析整个项目
- 读取所有文件
- Token: 50K-100K
- 耗时: 30-60秒

Git-Based + 分层文档:
- 读取10个commits
- 读取3个CLAUDE.md
- 读取CONTEXT.md
- Token: 5K-10K
- 耗时: < 5秒

节省: 80-90% Token, 90%+ 时间
```

---

## 实施计划

### Phase 1: Git-Based Memory (Week 1)
- [ ] 实现Git Context Controller
- [ ] 创建CONTEXT.md自动快照
- [ ] 实现Memory Commit机制
- [ ] 实现Context Recovery
- [ ] 测试Git branch探索

### Phase 2: Maintenance Mode (Week 1)
- [ ] 完善maintenance.md
- [ ] 实现ISSUES.md追踪
- [ ] 实现问题扫描
- [ ] 实现修复工作流
- [ ] 测试完整维护流程

### Phase 3: Hierarchical Docs (Week 2)
- [ ] 设计HIERARCHY.json
- [ ] 实现自动加载逻辑
- [ ] 创建文档模板
- [ ] 实现智能上下文组装
- [ ] 测试大型项目

### Phase 4: Multi-Agent Parallel (Week 2)
- [ ] 设计AGENTS.json
- [ ] 实现Agent Pool
- [ ] 实现并行调度
- [ ] 实现State Graph
- [ ] 测试并发场景

### Phase 5: Integration (Week 3)
- [ ] 集成所有系统
- [ ] 端到端测试
- [ ] 性能优化
- [ ] 文档完善
- [ ] 发布v2.0

---

## 参考资料

### Git-Based Memory
- [Git Context Controller](https://arxiv.org/html/2508.00031v1)
- [DiffMem](https://github.com/Growth-Kinetics/DiffMem)
- [Git Memory Hack](https://www.theaistack.dev/p/git-memory)

### Multi-Agent Systems
- [LangGraph Multi-Agent](https://latenode.com/blog/langgraph-multi-agent-orchestration-complete-framework-guide-architecture-analysis-2025)
- [OpenAI Agents SDK](https://openai.github.io/openai-agents-python/multi_agent/)
- [Azure AI Agent Patterns](https://learn.microsoft.com/en-us/azure/architecture/ai-ml/guide/ai-agent-design-patterns)

### Context Management
- [Augment for Large Codebases](https://theaieconomy.substack.com/p/meet-the-ai-agent-built-for-massive)
- [DocAgent](https://arxiv.org/html/2504.08725v1)
- [AI-Forge Dual Memory](https://www.ijraset.com/best-journal/ai-forge-your-coding-assistant)

### Memory Systems
- [Mem0](https://github.com/mem0ai/mem0)
- [MongoDB LangGraph](https://www.mongodb.com/company/blog/product-release-announcements/powering-long-term-memory-for-agents-langgraph)
- [AWS Bedrock AgentCore](https://aws.amazon.com/blogs/machine-learning/building-smarter-ai-agents-agentcore-long-term-memory-deep-dive/)

---

*创建于: 2025-12-24*
*版本: 2.0*
*基于: 2025年AI Agent最佳实践*
