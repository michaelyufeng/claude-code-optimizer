---
description: Hierarchical CLAUDE.md documentation for large projects
---

# 分层文档系统 (Hierarchical Documentation)

Claude Code **原生支持**分层 CLAUDE.md 文档，但很多人不知道这个功能！

## 🎯 解决的问题

### 传统单一文档的问题

```
❌ 大项目单一 CLAUDE.md 问题:
- 文档过长（>2000 行）超出上下文
- 无关模块信息干扰 Claude 理解
- 团队协作时文档冲突
- 每次都要读取整个文档
```

### 分层文档的优势

```
✅ 分层 CLAUDE.md 优势:
- 每个模块文档独立（100-200 行）
- Claude 自动加载相关层级
- 团队可以并行编辑
- 精准上下文，提升理解
```

---

## 📚 Claude Code 如何读取 CLAUDE.md

### 读取顺序

```
Claude Code 按照以下顺序读取和合并 CLAUDE.md:

1. ~/.claude/CLAUDE.md          # 全局配置（所有项目共享）
   ↓
2. /project/CLAUDE.md            # 项目级配置
   ↓
3. /project/subdir/CLAUDE.md     # 目录级配置
```

### 优先级规则

```
后读取的配置会覆盖前面的配置:

全局 CLAUDE.md: "使用 Python 3.11"
项目 CLAUDE.md: "使用 Python 3.12"  ✅ 生效
```

### 自动加载机制

**关键**: Claude 会根据**当前工作目录**自动加载相关的 CLAUDE.md！

```
你在工作:     Claude 会读取:
/project/     → ~/.claude/CLAUDE.md + /project/CLAUDE.md
/project/api/ → ~/.claude/CLAUDE.md + /project/CLAUDE.md + /project/api/CLAUDE.md
```

---

## 🏗️ 推荐架构

### 三层架构（推荐）

```
project/
├── CLAUDE.md                    # 第1层: 项目级（全局规则）
│   - 项目概述
│   - 技术栈
│   - 核心约定
│   - 文件结构
│   长度: 100-200 行
│
├── src/
│   ├── api/
│   │   └── CLAUDE.md            # 第2层: 模块级（模块说明）
│   │       - API 设计原则
│   │       - 路由规范
│   │       - 认证流程
│   │       长度: 100-150 行
│   │
│   ├── database/
│   │   └── CLAUDE.md            # 第2层: 模块级
│   │       - 数据模型
│   │       - 查询规范
│   │       - 迁移流程
│   │
│   └── trading/
│       ├── CLAUDE.md            # 第2层: 模块级
│       │   - 交易核心逻辑
│       │   - 子模块索引
│       │
│       ├── strategies/
│       │   └── CLAUDE.md        # 第3层: 子模块级（详细逻辑）
│       │       - 策略算法
│       │       - 参数配置
│       │       长度: 50-100 行
│       │
│       └── risk/
│           └── CLAUDE.md        # 第3层: 子模块级
│               - 风控规则
│               - 限额配置
```

### 两层架构（小项目）

```
project/
├── CLAUDE.md                    # 项目级
└── src/
    ├── frontend/
    │   └── CLAUDE.md            # 前端模块
    └── backend/
        └── CLAUDE.md            # 后端模块
```

---

## 📝 文档模板

### 第1层: 项目级 CLAUDE.md

```markdown
# [项目名称]

> 版本: 1.0.0 | 创建: 2025-12-24

## 项目概述

[一句话描述项目]

## 技术栈

**语言**: [Python/TypeScript/etc]
**框架**: [FastAPI/Next.js/etc]
**数据库**: [PostgreSQL/MongoDB/etc]

## 核心约定

### 代码风格
- [Python: PEP8]
- [TypeScript: Airbnb]

### 命名规范
- 文件: kebab-case
- 类: PascalCase
- 函数: camelCase

### 架构原则
- [DDD / Clean Architecture / etc]

## 目录结构

```
project/
├── src/
│   ├── api/          # API 层（见 src/api/CLAUDE.md）
│   ├── database/     # 数据层（见 src/database/CLAUDE.md）
│   └── trading/      # 业务层（见 src/trading/CLAUDE.md）
├── tests/
└── docs/
```

## 开发流程

1. 创建分支: `feature/xxx`
2. 开发 + 测试
3. 提交 PR
4. Code Review
5. 合并到 main

## 重要提醒

- 所有金额使用 Decimal
- 所有时间使用 UTC
- 禁止硬编码配置

## 相关文档

- 详细架构: docs/ARCHITECTURE.md
- API 文档: docs/API.md
- 部署指南: docs/DEPLOYMENT.md
```

### 第2层: 模块级 CLAUDE.md

**示例: src/api/CLAUDE.md**

```markdown
# API 模块

> 父文档: /CLAUDE.md

## 模块职责

提供 RESTful API 接口，处理 HTTP 请求。

## 文件结构

```
api/
├── routes/
│   ├── auth.py       # 认证路由
│   ├── trading.py    # 交易路由
│   └── market.py     # 行情路由
├── middleware/
│   ├── auth.py       # 认证中间件
│   └── rate_limit.py # 限流中间件
└── schemas/
    └── requests.py   # 请求模型
```

## API 设计规范

### 路由格式
```
/api/v1/{resource}/{action}

例:
POST /api/v1/orders/create
GET  /api/v1/orders/list
```

### 认证
- Header: `Authorization: Bearer {token}`
- Token 有效期: 24 小时

### 响应格式
```json
{
  "success": true,
  "data": {},
  "error": null,
  "timestamp": "2025-12-24T12:00:00Z"
}
```

### 错误处理
- 400: 参数错误
- 401: 未认证
- 403: 无权限
- 429: 请求过多
- 500: 服务器错误

## 关键逻辑

### 认证流程
1. 客户端发送 username + password
2. 验证用户
3. 生成 JWT token
4. 返回 token

### 限流策略
- 普通用户: 100 req/min
- VIP 用户: 1000 req/min

## 依赖的其他模块

- `src/database/`: 查询用户、订单数据
- `src/trading/`: 调用交易核心
```

### 第3层: 子模块级 CLAUDE.md

**示例: src/trading/strategies/CLAUDE.md**

```markdown
# 交易策略模块

> 父文档: /src/trading/CLAUDE.md

## 策略列表

1. **MA Cross** (moving_average.py)
   - 双均线交叉策略
   - 参数: fast_period, slow_period

2. **RSI** (rsi_strategy.py)
   - RSI 超买超卖策略
   - 参数: period, overbought, oversold

3. **MACD** (macd_strategy.py)
   - MACD 指标策略
   - 参数: fast, slow, signal

## 策略接口

所有策略必须实现:

```python
class BaseStrategy(ABC):
    @abstractmethod
    def analyze(self, market_data) -> Signal:
        """分析市场数据，返回交易信号"""
        pass

    @abstractmethod
    def backtest(self, historical_data) -> BacktestResult:
        """回测策略"""
        pass
```

## 策略参数配置

存储在: `config/strategies.yaml`

```yaml
ma_cross:
  fast_period: 10
  slow_period: 30

rsi:
  period: 14
  overbought: 70
  oversold: 30
```

## 添加新策略步骤

1. 继承 BaseStrategy
2. 实现 analyze() 和 backtest()
3. 添加配置到 strategies.yaml
4. 编写单元测试
5. 运行回测验证
```

---

## 🎨 实战示例: AI Trading 项目

### 完整文档结构

```
aitrading/
├── CLAUDE.md                      # 🟦 全局: 项目概述、技术栈
│
├── src/
│   ├── api/
│   │   ├── CLAUDE.md              # 🟩 模块: API 设计规范
│   │   ├── routes/
│   │   │   ├── auth.py
│   │   │   └── trading.py
│   │   └── middleware/
│   │
│   ├── database/
│   │   ├── CLAUDE.md              # 🟩 模块: 数据模型、查询
│   │   ├── models/
│   │   └── migrations/
│   │
│   ├── trading/
│   │   ├── CLAUDE.md              # 🟩 模块: 交易核心
│   │   │
│   │   ├── strategies/
│   │   │   ├── CLAUDE.md          # 🟨 子模块: 策略详情
│   │   │   ├── ma_cross.py
│   │   │   └── rsi_strategy.py
│   │   │
│   │   ├── risk/
│   │   │   ├── CLAUDE.md          # 🟨 子模块: 风控规则
│   │   │   ├── position_limit.py
│   │   │   └── drawdown_control.py
│   │   │
│   │   └── execution/
│   │       ├── CLAUDE.md          # 🟨 子模块: 订单执行
│   │       └── order_manager.py
│   │
│   └── market_data/
│       ├── CLAUDE.md              # 🟩 模块: 行情数据
│       └── providers/
│
└── tests/
    └── CLAUDE.md                  # 🟩 模块: 测试规范
```

### 各层内容分配

**🟦 全局 (200 行)**:
- 项目概述: 20 行
- 技术栈: 30 行
- 核心约定: 50 行
- 目录结构: 50 行
- 开发流程: 30 行
- 重要提醒: 20 行

**🟩 模块 (150 行)**:
- 模块职责: 20 行
- 文件结构: 30 行
- 设计规范: 40 行
- 关键逻辑: 40 行
- 依赖关系: 20 行

**🟨 子模块 (100 行)**:
- 详细逻辑: 60 行
- 配置说明: 20 行
- 示例代码: 20 行

---

## 💡 最佳实践

### 1. 保持文档简洁

```
✅ 好的文档:
- 100-200 行
- 重点突出
- 机器可读

❌ 坏的文档:
- 超过 500 行
- 大段叙述
- 过多解释
```

### 2. 使用层级索引

```markdown
# 项目级 CLAUDE.md

## 目录结构

```
src/
├── api/          # API 层（详见 src/api/CLAUDE.md）
├── database/     # 数据层（详见 src/database/CLAUDE.md）
└── trading/      # 业务层（详见 src/trading/CLAUDE.md）
```

提示 Claude 查看子文档。
```

### 3. 避免重复信息

```
❌ 错误: 每个 CLAUDE.md 都写技术栈

✅ 正确:
- 项目级: 写技术栈
- 模块级: 只写本模块特定的技术细节
```

### 4. 使用相对引用

```markdown
# 模块级 CLAUDE.md

> 父文档: /CLAUDE.md
> 相关: ../api/CLAUDE.md

这样 Claude 知道文档层级关系。
```

### 5. 版本控制

```markdown
# CLAUDE.md

> 版本: 1.2.0
> 最后更新: 2025-12-24
> 更新者: Claude + User

## 变更日志

### v1.2.0 (2025-12-24)
- 添加风控模块说明
- 更新策略参数配置

### v1.1.0 (2025-12-20)
- 添加 API 限流规则
```

---

## 🔧 工具命令

### 初始化分层文档

```bash
/project-optimizer:hierarchical-docs --init

# 生成推荐的文档结构:
# - 分析项目目录
# - 创建各层 CLAUDE.md 模板
# - 生成文档索引
```

### 验证文档结构

```bash
/project-optimizer:hierarchical-docs --validate

# 检查:
# - 文档层级是否合理
# - 长度是否超标
# - 是否有重复内容
```

### 生成文档索引

```bash
/project-optimizer:hierarchical-docs --index

# 生成 docs/CLAUDE_INDEX.md:
# - 所有 CLAUDE.md 位置
# - 各文档负责的模块
# - 文档之间的关系图
```

---

## 📖 使用指南

### 场景 1: 新项目初始化

```bash
# 1. 创建项目级文档
cat > CLAUDE.md << 'EOF'
# My Project
...基础信息...
EOF

# 2. 创建模块级文档
mkdir -p src/api
cat > src/api/CLAUDE.md << 'EOF'
# API Module
> 父文档: /CLAUDE.md
...API 说明...
EOF

# 3. 让 Claude 验证
/project-optimizer:hierarchical-docs --validate
```

### 场景 2: 已有项目迁移

```bash
# 1. 分析现有 CLAUDE.md
/project-optimizer:hierarchical-docs --analyze

# 输出建议:
# - CLAUDE.md 过长 (800 行)
# - 建议拆分为 4 个文档
# - 推荐结构: ...

# 2. 自动拆分
/project-optimizer:hierarchical-docs --split

# 3. 验证结果
/project-optimizer:hierarchical-docs --validate
```

### 场景 3: 工作在特定模块

```bash
# 假设您在修复 trading 模块的问题

# Claude 会自动加载:
# 1. ~/.claude/CLAUDE.md          (如果有)
# 2. /project/CLAUDE.md           (项目全局)
# 3. /project/src/trading/CLAUDE.md (当前模块)

# 不会加载:
# - src/api/CLAUDE.md             (无关模块)
# - src/database/CLAUDE.md        (无关模块)

# 这样 Claude 专注于 trading 模块的上下文！
```

---

## 🆚 对比其他方案

### vs 单一大文档

| 特性 | 单一 CLAUDE.md | 分层 CLAUDE.md |
|------|---------------|---------------|
| 长度 | 2000+ 行 | 100-200 行/层 |
| 上下文 | 全部加载 | 按需加载 |
| 维护 | 困难（冲突多） | 简单（独立编辑） |
| 精准度 | 低（信息过多） | 高（精准上下文） |

### vs 代码注释

| 特性 | 代码注释 | CLAUDE.md |
|------|---------|----------|
| 位置 | 分散在代码 | 集中说明 |
| 结构 | 碎片化 | 结构化 |
| 全局规则 | 难以表达 | 清晰表达 |
| Claude 理解 | 需要读代码 | 直接读文档 |

---

## 🔗 相关功能

- `/project-optimizer:maintenance` - 维护模式（使用分层文档）
- `/project-optimizer:diary` - 记忆系统（会引用文档）
- `/project-optimizer:sync` - 同步系统（追踪文档变更）

---

## 📚 参考资料

Sources:
- [Managing Claude Code's Context](https://www.cometapi.com/managing-claude-codes-context/)
- [Claude Code Best Practices](https://www.anthropic.com/engineering/claude-code-best-practices)
- [How I Use Every Claude Code Feature](https://blog.sshh.io/p/how-i-use-every-claude-code-feature)

---

*分层文档是大型项目成功的关键！*
